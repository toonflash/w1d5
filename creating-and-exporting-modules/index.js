var createList = require("./sort-module");
/*
Thi takes a number that will be put into an array
and then 4 ascending numbers added to that list.
Then that list is sorted into a decsending order
*/
console.log(createList.takeANumber(8)); 
